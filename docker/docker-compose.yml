version: '3.8'

services:
  ai-vault:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "${API_PORT:-3000}:3000"
    env_file:
      - ../.env
    volumes:
      - ../vault:/app/vault
      - ai-vault-data:/app/data
    restart: unless-stopped

volumes:
  ai-vault-data:
